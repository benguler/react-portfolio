import React from 'react';

import {Button, Box} from '@mui/material/';

const LoadMore = ({video, onVideoSelect}) => {
  return(
    <Box>
      <Button variant="outlined">Load More</Button>
    </Box>
  );

}

export default LoadMore;
