import React from 'react';
import {Typography} from '@mui/material/';

const Header = () => {
  return (
    <div>
      <Typography variant="h2">To Do Tracker</Typography>
    </div>

  );

};

export default Header;
